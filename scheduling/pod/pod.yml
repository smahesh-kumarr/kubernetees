apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    name: nginx-25
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80
      name: http
      protocol: TCP

